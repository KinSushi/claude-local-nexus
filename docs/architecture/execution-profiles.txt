# Execution Profiles

profiles:

coding:
task_class:
- code
- debugging
- refactoring

```
requirements:
  context_min: 65536
  tool_use: required
  repository_navigation: required

privacy:
  default: local_preferred

preferred_models:
  - qwen3-coder-30b-local
  - glm-4.7-flash-local
  - claude-sonnet-5
```

deep-reasoning:
task_class:
- architecture
- mathematical_reasoning
- difficult_debugging

```
requirements:
  reasoning: high
  context_min: 32768

preferred_models:
  - claude-opus-5
  - claude-sonnet-5
```

lightweight:
task_class:
- classification
- extraction
- transformation

```
requirements:
  context_min: 8192

preferred_models:
  - gemma4-12b-local
  - llama3.2-3b-local
  - phi3-mini-local
```

multimodal:
task_class:
- image
- screenshot
- OCR
- document

```
requirements:
  vision: required

preferred_models:
  - llama3.2-vision-11b-local
  - llama3.2-vision-90b-local
  - claude-sonnet-5
```

[CORRECTION 2026-09-02, audit E-M — FAUX] `llama3.2-vision-90b-local` n'existe
QUE dans l'inventaire declare (`model_list.txt:14`, `llama3.2-vision:90b`) ;
il n'a AUCUN alias `model_name` dans `litellm_config.yaml` (grep confirme :
seul `llama3.2-vision-11b-local` y est expose, `litellm_config.yaml:374`).
Selon la distinction du contrat (§6, installe/declare/expose) c'est un modele
DECLARE mais PAS EXPOSE : le nommer dans un profil d'execution comme s'il
etait routable produirait un alias invalide au premier appel reel.


