version: 1

defaults:

privacy:
classification: L1

routing:
local_preferred: true

fallback:
allow_cross_provider: false

execution:
autonomous: true

rules:

* id: L3-local-only
  when:
  classification: L3
  allow:
  providers:
  - ollama-local
  deny:
  providers:
  - ollama-cloud
  - anthropic

* id: code-large-context
  when:
  task_class: coding
  context_tokens: ">65536"
  require:
  context_profile: coding-large

* id: multimodal
  when:
  modality: image
  require:
  capability:
  vision: true

* id: sensitive-tools
  when:
  tool_class: destructive
  require:
  human_approval: true

[VERIFICATION 2026-09-02, audit E-M — NON IMPLEMENTE] Ce fichier est un
brouillon de schema, pas une configuration active : aucun fichier
`execution-policy.yaml` (sans le suffixe `.txt`) n'existe ailleurs dans le
depot (recherche exhaustive), et aucun script ne parse une cle `rules:` de
cette forme (grep sur `L3`, `human_approval`, `tool_class` dans `scripts/*.py`
ne trouve qu'un commentaire isole mentionnant L2/L3 dans
`scripts/nexus_test.py:777`, sans moteur de regles). Les principes qu'il
enonce (L3 local-only, approbation humaine pour le destructif) concordent
avec la doctrine deployee (Claude.md §35, §108) mais celle-ci est appliquee
par des controles distincts (`Test-NexusConfig.ps1`, les hooks
`.claude/settings.json`) et non par ce schema YAML.
