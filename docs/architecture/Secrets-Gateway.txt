Secrets Gateway

Même logique pour les secrets.

Le modèle ne devrait idéalement jamais recevoir :

ANTHROPIC_API_KEY
OLLAMA_CLOUD_API_KEY
POSTGRES_PASSWORD

en clair.

Le programme obtient un handle :

secret://anthropic/api-key

et la couche d'exécution résout le secret.

C'est une évolution importante si tu veux transformer la plateforme en control plane IA sérieux.

---
VERIFICATION (2026-09-02, agent d'audit, worktree agent-a268d2e8fe1dc957c)

ATTENTION PARTICULIERE DEMANDEE SUR CE FICHIER (contrat §34 : aucun secret
en clair dans un fichier suivi). CONTROLE EFFECTUE, RESULTAT PROPRE :

    $ grep -nE "sk-|api[_-]?key\s*[:=]\s*['\"a-zA-Z0-9]|password\s*[:=]|token\s*[:=]" \
        docs/architecture/Secrets-Gateway.txt
    (aucun resultat)

Seuls des NOMS de variable d'environnement figurent (`ANTHROPIC_API_KEY`,
`OLLAMA_CLOUD_API_KEY`, `POSTGRES_PASSWORD`), jamais une valeur. Conforme.
Aucune valeur n'est recopiee dans cette verification non plus.

MECANISME `secret://...` - NON DEPLOYE, ET LE FICHIER LE DIT LUI-MEME AU
CONDITIONNEL ("évolution importante SI tu veux..."). Verifie qu'aucune
implementation n'existe deja sous ce nom :

    $ grep -rn "secret://" scripts/*.py litellm_config.yaml
    (aucun resultat)

L'etat reel du depot est celui decrit par `.claude/CLAUDE.md` §20 : les
secrets vivent dans `.env` (non suivi), lus comme variables d'environnement
-- exactement l'etape que ce fichier propose de depasser, pas encore
l'etape suivante.

CONFRONTATION AUX DEUX OUVRAGES - CONCORDANCE, ET ELLE SITUE PRECISEMENT LE
DEPOT DANS LA PROGRESSION DECRITE. *Context Engineering for Multi-Agent
Systems*, section "Environment configuration and secrets management"
(fragment verifie verbatim, offset 11662630, epub) :

    "At runtime, the Context Engine reads its configuration dynamically.
    [...] import os [...] PINECONE_API_KEY = os.environ.get(...)
    [...] However, storing sensitive information directly in environment
    variables on the host machine or within basic Kubernetes Secrets is
    often insufficient for enterprise-gr[ade]..."

Ce depot est exactement a l'etape que le livre nomme suffisante pour du
prototypage mais insuffisante en production ("os.environ" + `.env`) : la
proposition `secret://` de ce fichier est la suite logique que le meme
ouvrage appelle de ses voeux, sans toutefois la nommer ainsi. *Agentic AI
for Offensive Cybersecurity* couvre le meme terrain sous "Secrets
management" et "External secrets managers" (resumes verifies dans l'index,
texte non relu verbatim par manque de temps dans cette passe).