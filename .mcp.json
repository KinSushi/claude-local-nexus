{
  "mcpServers": {
    "nexus-local": {
      "command": "node",
      "args": ["${CLAUDE_PROJECT_DIR:-.}/tools/nexus-mcp/server.js"],
      "env": {
        "NEXUS_LITELLM_URL": "${NEXUS_LITELLM_URL:-http://127.0.0.1:4000}"
      }
    }
  }
}
